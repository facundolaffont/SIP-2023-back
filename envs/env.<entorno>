En esta carpeta, y reemplazando <entorno> por 'local', 'cloud-dev' o 'cloud-prod', por ejemplo,
se pueden guardar los archivos de configuración de los diferentes entornos. Con dichos formatos
no se compartiría el archivo en GitHub.

En caso de querer trabajar en el entorno local, el contenido del archivo con las correspondientes
configuraciones reemplazará al de backend/.env.

En caso de querer trabajar en la nube, el contenido del archivo con las correspondientes configuraciones
reemplazará al de backend/src/main/resources/.env.